{
  "name": "action-fetch-secret",
  "version": "1.0.0",
  "description": "Fetches a secret from AWS Secrets Manager and converts it to a .env",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^4.0.0",
    "aws-sdk": "^2.866.0"
  }
}
